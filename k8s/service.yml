apiVersion: v1
kind: Service
metadata:
  name: incert
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "incert"
    port: 8080
  selector:
    app: incert